/*
* Auth: Gbemi Jacob Adebayo
*/

#include "myheaders.h"

/**
* _islower - Checks if a character is lowercase.
*
*/

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
